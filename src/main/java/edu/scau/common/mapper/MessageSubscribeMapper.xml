<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.scau.common.mapper.MessageSubscribeMapper">
    <select id="getUserId" resultType="integer">
        select id from `user`
    </select>
    <select id="getOpenId" resultType="string" parameterType="int">
        select open_id from message_subscribe
        where user_id=#{userId}
    </select>
    <select id="getIsOpen" resultType="boolean" parameterType="int">
        select is_open from message_subscribe
        where user_id=#{userId}
    </select>
    <select id="getId" resultType="integer" parameterType="int">
        select user_id from message_subscribe
        where user_id=#{userId}
    </select>
    <insert id="insertOpenId">
        insert into message_subscribe(user_id, open_id, is_open)
        VALUES(#{userId},#{openId},1)
    </insert>
    <update id="updateOpen" parameterType="int">
        update message_subscribe
        set is_open=NOT is_open
        where user_id=#{userId}
    </update>
    <select id="getActivityId" parameterType="int" resultType="integer">
        select activity_id from message_subscribe_allow
        where user_id=#{userId}
    </select>
    <insert id="insertMessageSubscribeAllow">
        insert into message_subscribe_allow(user_id, activity_id) value (#{userId},#{activityId})
    </insert>
    <select id="getMessageSubscribeAllowId" resultType="integer">
        select id from message_subscribe_allow
        where user_id=#{userId}
        and activity_id=#{activityId}
    </select>
    <delete id="deleteMessageSubscribeAllow">
        delete from message_subscribe_allow
        where user_id=#{userId}
        and activity_id=#{activityId}
    </delete>
</mapper>